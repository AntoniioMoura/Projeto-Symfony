{% extends 'base.html.twig' %}
{% block body %}
    <div class="text-center"><h2>Editar Pizza</h2></div>
    <br>
    <div class="container">
        <div class="col-mds-12">
            {% include '@App/includes/messages.html.twig' %}
        </div>
        <div class="col-md-6">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn-lg btn btn-success">Concluir</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}