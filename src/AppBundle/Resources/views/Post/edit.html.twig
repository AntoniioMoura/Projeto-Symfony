{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">

        <div class="col-md-6">
            <h1>Editar</h1>
            <br>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn-lg btn btn-success">Cadastrar</button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}